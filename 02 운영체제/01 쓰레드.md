
- 운영체제 쓰레드
- 운영체제 쓰레드와 코어, 실제로 어떻게 동작할지
- JVM 스레드와 OS스레드의 관계

---

**쓰레드는 CPU 이용의 기본 단위이다.**

새 프로세스가 해야 할 일이 기존 프로세스가 하는 일과 동일하다면 많은 오버헤드를 감수할 필요가 있을까? 그것 보다는 프로세스 안에 여러 스레드를 만들어 나가는 것이 더 효율적이다.

### 다중 쓰레드 사용의 장점

- 응답성
	- 한 스레드가 블록되어도 다른 스레드는 계속 실행
- 자원공유
	- 프로세스안의 스레드는 애초에 코드와 데이터를 공유한다. 
	- 공유 메모리와 메시지 전달을 하는 프로세스간 자원공유에 비하면 훨씬 간단하다.
- 경제성
	- 프로세스에 비해 생성 오버헤드가 훨씬 적다. 자원을 공유하기 때문에
- 규모적응성
	- 다중코어 시스템을 더 잘 활용할 수 있음
	- 단일코어에서 스레드가 인터리브되는 것은 병행성을 제공할 뿐, 병렬성은 아니다.

### 쓰레드 모델

대부분의 운영체제는 사용자공간과 커널공간에 일대일 쓰레드 모델을 사용한다. 
사용자 공간 N, 커널공간 1개는 멀티코어 이점을 못살린다.
다대다 모델은 구현이 어렵다. 그리고 처리 코어 수가 증가하면서 커널 스레드를 제한하는 것의 중요성이 줄어들었다.